import { useToast } from "@/hooks/use-toast";

const Home = () => {
  const { toast } = useToast();

  return <div className="min-h-screen bg-background">{/* Hero Section */}</div>;
};

export default Home;
