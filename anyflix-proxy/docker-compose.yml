version: '3.8'

services:
  anyflix-proxy:
    build: .
    container_name: anyflix-proxy
    ports:
      - "8081:8081"
    environment:
      - PORT=8081
    restart: unless-stopped
    networks:
      - anyflix-network

networks:
  anyflix-network:
    driver: bridge

